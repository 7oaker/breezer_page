---
# Copy this file to src/content/blog/en/<slug>.md (or .../de/<slug>.md).
# Files starting with "_" are excluded from the build.
# Every field below is validated at build time — the build fails loudly rather
# than shipping a page with a missing or over-long description.

# <title>. 10–75 chars. Lead with the keyword.
title: "Keyword-first title under 60 characters"
# <h1>. May differ from the title; must contain the target keyword.
heading: "The headline readers see on the page"
# <meta name="description">. 50–175 chars, but aim for <=160 so Google doesn't truncate.
description: "One sentence that answers the search query and contains the target keyword, kept under 160 characters."
# og:description + the card text on /blog. 40–200 chars.
summary: "Short summary used on the blog listing card and social previews."
lang: en
publishDate: 2026-01-01
# updatedDate: 2026-02-01
# Slug of the counterpart post in the other language. Omit if not translated —
# hreflang tags are only emitted when a real pair exists.
# translationOf: mein-slug
# author: Breezer Team

# Optional. Rendered as a visible FAQ section AND as FAQPage schema from this
# one array, so the two can never drift apart.
# faq:
#   - q: "A question people actually type into Google"
#     a: "A direct answer in 2–3 sentences."
---

Open with the answer. AI assistants and featured snippets both lift the first
one or two sentences, so put the payoff here rather than building up to it.

## Use H2s that read like questions people ask

Body copy in Markdown. Link into the money pages at least twice — that is the
main SEO job of a post: [snus tracker](/snus-tracker), [Quit Mode](/quit-snus),
[Zyn tracker](/zyn-tracker), [app comparison](/vs-snusless).

- Bullets and **bold** work as normal
- So do tables, quotes and code

## Close with something concrete

The store buttons, FAQ section and "all articles" link are added automatically
by the layout — don't repeat them here.
